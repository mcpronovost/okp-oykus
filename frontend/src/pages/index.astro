---
import { getLang, languages, defaultLang } from "@/plugins/i18n/utils.js";

const lang = getLang(Astro.url);
const currentLang = languages[lang] ? lang : defaultLang;

return Astro.redirect(`/${currentLang}/`);
---
