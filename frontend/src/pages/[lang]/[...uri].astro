---
import { getLang } from "@/plugins/i18n/utils.js";
import { getRoute } from "@/plugins/router/utils.js";

const { uri } = Astro.params;
const lang = getLang(Astro.url);

const route = getRoute(`/${uri||""}`, lang);
const Template = route.view();
---

<Template meta={route.meta} />
