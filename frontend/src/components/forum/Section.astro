---
import type { Section } from "@/types/forums.types";
import ForumHeader from "./common/Header.astro";
import OkpTopicsList from "./react/TopicsList.jsx";

interface Props {
  section: Section;
}

const { slug } = Astro.params;
const { section }: Props = Astro.props;
---

<article class="okp-forum-section">
  <ForumHeader title={section.name} description={section.description} />
  <OkpTopicsList slug={slug} section={section} client:only="react" />
</article>
